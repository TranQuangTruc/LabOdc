spring:
  application:
    name: TaskAndReporting-service
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/lab
    username: root
    password: 1234

server:
  port: 8081
  servlet:
    context-path: /api
